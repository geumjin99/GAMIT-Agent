# Pinned lower bounds so a fresh Docker build stays on compatible APIs.
# openai>=1.0 is required (the engine uses the OpenAI() client + chat.completions).
fastapi>=0.110,<1.0
uvicorn>=0.27
sse-starlette>=1.8
openai>=1.30,<2.0
python-dotenv>=1.0
requests>=2.31
